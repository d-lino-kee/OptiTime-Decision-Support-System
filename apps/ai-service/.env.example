AI_SERVICE_HMAC_SECRET=dev_ai_hmac_change_me
HMAC_MAX_SKEW_SECONDS=300

WEAVIATE_URL=http://localhost:8080
WEAVIATE_API_KEY=

# Toggle to enable real ML pipelines + Weaviate. Defaults to lightweight mode
# so local dev does not require GPUs or model downloads.
ENABLE_WEAVIATE=false
ENABLE_REAL_MODELS=false
