
---

## `docs/local-dev.md`
```markdown
# Local Development

## Prerequisites
- Node.js 18+ (or 20+)
- pnpm (recommended) or npm
- Docker + Docker Compose
- Python 3.11+

## 1) Start dependencies
From repo root:
```bash
docker compose up -d