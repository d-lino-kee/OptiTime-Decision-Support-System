MONGODB_URI=mongodb://localhost:27017/optitime
REDIS_URL=redis://localhost:6379
WEAVIATE_URL=http://localhost:8080
AI_SERVICE_URL=http://localhost:8000
AI_SERVICE_HMAC_SECRET=dev_ai_hmac_change_me
FIREBASE_PROJECT_ID=optitime-ai
GOOGLE_APPLICATION_CREDENTIALS=./secrets/firebase-admin.json

# Local dev only. Something on this machine (firewall/AV) intercepts HTTPS with a
# CA Node does not trust, so we relax TLS verification for outbound calls. Do NOT
# set this in any deployed environment.
NODE_TLS_REJECT_UNAUTHORIZED=0